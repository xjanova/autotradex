<Window x:Class="AutoTradeX.UI.Views.ScannerActionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:AutoTradeX.UI.Controls"
        Title="เลือกการดำเนินการ"
        Height="420" Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Border CornerRadius="16" BorderThickness="1" BorderBrush="#40FFFFFF">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#1A0A2E" Offset="0"/>
                <GradientStop Color="#0A0A1A" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>
        <Border.Effect>
            <DropShadowEffect BlurRadius="30" ShadowDepth="0" Opacity="0.8" Color="#000000"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Padding="20,15" Background="#10FFFFFF" CornerRadius="16,16,0,0"
                    MouseLeftButtonDown="Header_MouseLeftButtonDown">
                <Grid>
                    <TextBlock Text="เลือกการดำเนินการ" FontSize="16" FontWeight="Bold" Foreground="White"/>
                    <Button Content="✕" HorizontalAlignment="Right" Cursor="Hand" Click="CloseButton_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="bg" Background="Transparent" Padding="8,4" CornerRadius="6">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="bg" Property="Background" Value="#30FFFFFF"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                        <Button.Foreground>
                            <SolidColorBrush Color="#80FFFFFF"/>
                        </Button.Foreground>
                    </Button>
                </Grid>
            </Border>

            <!-- Selected Coin Info -->
            <Border Grid.Row="1" Background="#15FFFFFF" Margin="20,15,20,0" CornerRadius="12" Padding="15">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <controls:CoinIcon x:Name="CoinIconDisplay" Size="48" Margin="0,0,12,0"/>

                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock x:Name="SymbolText" Text="BTC/USDT" FontSize="18" FontWeight="Bold" Foreground="White"/>
                        <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                            <TextBlock x:Name="BuyExchangeText" Text="Binance" FontSize="12" Foreground="#F3BA2F"/>
                            <TextBlock Text=" → " FontSize="12" Foreground="#60FFFFFF"/>
                            <TextBlock x:Name="SellExchangeText" Text="KuCoin" FontSize="12" Foreground="#23AF91"/>
                        </StackPanel>
                    </StackPanel>

                    <StackPanel Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right">
                        <Border Background="#207C3AED" CornerRadius="8" Padding="10,4">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Score: " FontSize="11" Foreground="#80FFFFFF"/>
                                <TextBlock x:Name="ScoreText" Text="85" FontSize="14" FontWeight="Bold" Foreground="#7C3AED"/>
                            </StackPanel>
                        </Border>
                        <TextBlock x:Name="SpreadText" Text="0.15%" FontSize="14" FontWeight="SemiBold"
                                   Foreground="#10B981" HorizontalAlignment="Right" Margin="0,6,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Action Options -->
            <StackPanel Grid.Row="2" Margin="20,15">
                <TextBlock Text="คุณต้องการทำอะไรกับคู่เทรดนี้?" FontSize="13" Foreground="#80FFFFFF" Margin="0,0,0,15"/>

                <!-- Add to Project Button -->
                <Button x:Name="AddToProjectButton" Click="AddToProjectButton_Click" Cursor="Hand" Margin="0,0,0,10">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" Background="#15FFFFFF" CornerRadius="12" Padding="15"
                                    BorderBrush="#30FFFFFF" BorderThickness="1">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <Border Background="#2010B981" CornerRadius="10" Width="44" Height="44">
                                        <TextBlock Text="📁" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>

                                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="15,0,0,0">
                                        <TextBlock Text="เพิ่มเข้าโปรเจค" FontSize="14" FontWeight="SemiBold" Foreground="White"/>
                                        <TextBlock Text="บันทึกคู่เทรดนี้เพื่อใช้งานภายหลัง" FontSize="11" Foreground="#60FFFFFF"/>
                                    </StackPanel>

                                    <TextBlock Grid.Column="2" Text="→" FontSize="18" Foreground="#10B981" VerticalAlignment="Center"/>
                                </Grid>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#2010B981"/>
                                    <Setter TargetName="border" Property="BorderBrush" Value="#10B981"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <!-- Execute Trade Button -->
                <Button x:Name="ExecuteTradeButton" Click="ExecuteTradeButton_Click" Cursor="Hand" Margin="0,0,0,10">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" Background="#15FFFFFF" CornerRadius="12" Padding="15"
                                    BorderBrush="#30FFFFFF" BorderThickness="1">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <Border Background="#207C3AED" CornerRadius="10" Width="44" Height="44">
                                        <TextBlock Text="⚡" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>

                                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="15,0,0,0">
                                        <TextBlock Text="เทรดทันที" FontSize="14" FontWeight="SemiBold" Foreground="White"/>
                                        <TextBlock Text="ดำเนินการเทรดตอนนี้เลย" FontSize="11" Foreground="#60FFFFFF"/>
                                    </StackPanel>

                                    <TextBlock Grid.Column="2" Text="→" FontSize="18" Foreground="#7C3AED" VerticalAlignment="Center"/>
                                </Grid>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#207C3AED"/>
                                    <Setter TargetName="border" Property="BorderBrush" Value="#7C3AED"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <!-- View Details Button -->
                <Button x:Name="ViewDetailsButton" Click="ViewDetailsButton_Click" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" Background="#10FFFFFF" CornerRadius="12" Padding="15"
                                    BorderBrush="#20FFFFFF" BorderThickness="1">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <Border Background="#2000D4FF" CornerRadius="10" Width="44" Height="44">
                                        <TextBlock Text="📊" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>

                                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="15,0,0,0">
                                        <TextBlock Text="ดูรายละเอียด" FontSize="14" FontWeight="SemiBold" Foreground="White"/>
                                        <TextBlock Text="ดูข้อมูลราคาและ spread แบบละเอียด" FontSize="11" Foreground="#60FFFFFF"/>
                                    </StackPanel>

                                    <TextBlock Grid.Column="2" Text="→" FontSize="18" Foreground="#00D4FF" VerticalAlignment="Center"/>
                                </Grid>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#2000D4FF"/>
                                    <Setter TargetName="border" Property="BorderBrush" Value="#00D4FF"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>

            <!-- Footer -->
            <Border Grid.Row="3" Background="#08FFFFFF" Padding="20,12" CornerRadius="0,0,16,16">
                <Button Content="ยกเลิก" Click="CancelButton_Click" HorizontalAlignment="Center" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="bg" Background="Transparent" Padding="20,8" CornerRadius="8">
                                <ContentPresenter HorizontalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="bg" Property="Background" Value="#20FFFFFF"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                    <Button.Foreground>
                        <SolidColorBrush Color="#80FFFFFF"/>
                    </Button.Foreground>
                </Button>
            </Border>
        </Grid>
    </Border>
</Window>
