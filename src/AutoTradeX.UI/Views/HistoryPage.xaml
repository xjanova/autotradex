<UserControl x:Class="AutoTradeX.UI.Views.HistoryPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF">

    <UserControl.Resources>
        <!-- Styles -->
        <Style x:Key="StatCard" TargetType="Border">
            <Setter Property="Background" Value="#1A1A2E"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="8"/>
        </Style>

        <Style x:Key="SectionHeader" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>

        <!-- Premium ScrollBar Thumb Style -->
        <Style x:Key="PremiumScrollBarThumb" TargetType="Thumb">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Grid>
                            <!-- Outer glow effect -->
                            <Border x:Name="glowBorder" CornerRadius="5" MinHeight="40" Opacity="0">
                                <Border.Background>
                                    <RadialGradientBrush>
                                        <GradientStop Color="#508B5CF6" Offset="0"/>
                                        <GradientStop Color="#00000000" Offset="1"/>
                                    </RadialGradientBrush>
                                </Border.Background>
                            </Border>
                            <!-- Main thumb -->
                            <Border x:Name="thumb" CornerRadius="5" MinHeight="40" Width="8" HorizontalAlignment="Center">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                        <GradientStop Color="#8B5CF6" Offset="0"/>
                                        <GradientStop Color="#7C3AED" Offset="0.3"/>
                                        <GradientStop Color="#6D28D9" Offset="0.7"/>
                                        <GradientStop Color="#8B5CF6" Offset="1"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="8" ShadowDepth="0" Opacity="0.6" Color="#7C3AED"/>
                                </Border.Effect>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="thumb" Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                            <GradientStop Color="#A78BFA" Offset="0"/>
                                            <GradientStop Color="#8B5CF6" Offset="0.3"/>
                                            <GradientStop Color="#7C3AED" Offset="0.7"/>
                                            <GradientStop Color="#A78BFA" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="thumb" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect BlurRadius="12" ShadowDepth="0" Opacity="0.8" Color="#8B5CF6"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="glowBorder" Property="Opacity" Value="0.5"/>
                            </Trigger>
                            <Trigger Property="IsDragging" Value="True">
                                <Setter TargetName="thumb" Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                            <GradientStop Color="#C4B5FD" Offset="0"/>
                                            <GradientStop Color="#A78BFA" Offset="0.3"/>
                                            <GradientStop Color="#8B5CF6" Offset="0.7"/>
                                            <GradientStop Color="#C4B5FD" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="thumb" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect BlurRadius="16" ShadowDepth="0" Opacity="1" Color="#A78BFA"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="glowBorder" Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Premium ScrollBar Style -->
        <Style x:Key="PremiumScrollBar" TargetType="ScrollBar">
            <Setter Property="Width" Value="10"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid x:Name="Root" Width="10" Margin="2,4,6,4">
                            <!-- Track Background with glow -->
                            <Border Background="#15FFFFFF" CornerRadius="5" Width="8" HorizontalAlignment="Center">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="3" ShadowDepth="0" Opacity="0.2" Color="#000000"/>
                                </Border.Effect>
                            </Border>
                            <Track x:Name="PART_Track" IsDirectionReversed="True" Width="10">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageUpCommand">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton">
                                                <Border Background="Transparent"/>
                                            </ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageDownCommand">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton">
                                                <Border Background="Transparent"/>
                                            </ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.IncreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource PremiumScrollBarThumb}" Width="8"/>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Premium ScrollViewer Style for Lists -->
        <Style x:Key="PremiumScrollViewer" TargetType="ScrollViewer">
            <Setter Property="CanContentScroll" Value="False"/>
            <Setter Property="HorizontalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollViewer">
                        <Grid Background="{TemplateBinding Background}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <ScrollContentPresenter x:Name="PART_ScrollContentPresenter"
                                                    Grid.Column="0"
                                                    CanContentScroll="False"
                                                    ContentTemplate="{TemplateBinding ContentTemplate}"
                                                    Content="{TemplateBinding Content}"
                                                    Margin="{TemplateBinding Padding}"/>
                            <ScrollBar x:Name="PART_VerticalScrollBar"
                                       Grid.Column="1"
                                       Orientation="Vertical"
                                       Style="{StaticResource PremiumScrollBar}"
                                       Minimum="0"
                                       Maximum="{TemplateBinding ScrollableHeight}"
                                       Value="{TemplateBinding VerticalOffset}"
                                       ViewportSize="{TemplateBinding ViewportHeight}"
                                       Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ListView Style with Premium ScrollBar -->
        <Style x:Key="PremiumListView" TargetType="ListView">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.CanContentScroll" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListView">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer Style="{StaticResource PremiumScrollViewer}" Padding="0">
                                <ItemsPresenter/>
                            </ScrollViewer>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Premium Dark ComboBox Style -->
        <Style x:Key="DarkComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="#1E1E35"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#50FFFFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="14,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="MinHeight" Value="40"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="border" Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="10">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="8" ShadowDepth="0" Opacity="0.2" Color="#000000"/>
                                </Border.Effect>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="35"/>
                                    </Grid.ColumnDefinitions>
                                    <ContentPresenter Grid.Column="0"
                                                     Content="{TemplateBinding SelectionBoxItem}"
                                                     ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                     VerticalAlignment="Center"
                                                     Margin="14,0,0,0"/>
                                    <Border Grid.Column="1" Background="#20FFFFFF" CornerRadius="0,10,10,0" Margin="0,1,1,1">
                                        <Path Data="M0,0 L5,5 L10,0"
                                              Stroke="#FFFFFF"
                                              StrokeThickness="2"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                                    </Border>
                                    <ToggleButton Grid.ColumnSpan="2"
                                                 IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                 Background="Transparent"
                                                 BorderThickness="0"
                                                 Focusable="False"
                                                 ClickMode="Press"/>
                                </Grid>
                            </Border>
                            <Popup x:Name="PART_Popup"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   Placement="Bottom"
                                   AllowsTransparency="True"
                                   PopupAnimation="Slide">
                                <Border Background="#1E1E35"
                                        BorderBrush="#7C3AED"
                                        BorderThickness="1"
                                        CornerRadius="10"
                                        Margin="0,4,0,0"
                                        MinWidth="{TemplateBinding ActualWidth}">
                                    <Border.Effect>
                                        <DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.6" Color="#000000"/>
                                    </Border.Effect>
                                    <ScrollViewer MaxHeight="250" VerticalScrollBarVisibility="Auto" Margin="4">
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#7C3AED"/>
                                <Setter TargetName="border" Property="Background" Value="#282845"/>
                            </Trigger>
                            <Trigger Property="IsDropDownOpen" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#8B5CF6"/>
                                <Setter TargetName="border" Property="Background" Value="#282845"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="Padding" Value="14,12"/>
                        <Setter Property="FontSize" Value="14"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ComboBoxItem">
                                    <Border x:Name="border" Background="{TemplateBinding Background}"
                                            Padding="{TemplateBinding Padding}"
                                            CornerRadius="6" Margin="2">
                                        <ContentPresenter/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="#353560"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="border" Property="Background">
                                                <Setter.Value>
                                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                                        <GradientStop Color="#407C3AED" Offset="0"/>
                                                        <GradientStop Color="#207C3AED" Offset="1"/>
                                                    </LinearGradientBrush>
                                                </Setter.Value>
                                            </Setter>
                                            <Setter Property="Foreground" Value="#A78BFA"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="#0D0D1A">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#12121F" Padding="24,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Trade History" FontSize="24" FontWeight="Bold" Foreground="#FFFFFF"/>
                    <TextBlock Text="View all your trading activity and performance analytics"
                               FontSize="13" Foreground="#808080" Margin="0,4,0,0"/>
                </StackPanel>

                <!-- Filter Controls -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <ComboBox x:Name="TimeFilterCombo" Width="150" Margin="0,0,12,0"
                              Style="{StaticResource DarkComboBox}"
                              SelectionChanged="TimeFilter_SelectionChanged">
                        <ComboBoxItem Content="Today" IsSelected="True"/>
                        <ComboBoxItem Content="Last 7 Days"/>
                        <ComboBoxItem Content="Last 30 Days"/>
                        <ComboBoxItem Content="All Time"/>
                    </ComboBox>

                    <ComboBox x:Name="PairFilterCombo" Width="150" Margin="0,0,16,0"
                              Style="{StaticResource DarkComboBox}"
                              SelectionChanged="PairFilter_SelectionChanged">
                        <ComboBoxItem Content="All Pairs" IsSelected="True"/>
                    </ComboBox>

                    <Button x:Name="ExportButton" Content="📥 Export CSV"
                            Background="#8B5CF6" Foreground="#FFFFFF"
                            FontSize="13" FontWeight="SemiBold"
                            Padding="20,10" BorderThickness="0"
                            Cursor="Hand"
                            Click="ExportButton_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="btnBorder" CornerRadius="10" Padding="{TemplateBinding Padding}">
                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#7C3AED" Offset="0"/>
                                            <GradientStop Color="#6D28D9" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.Background>
                                    <Border.Effect>
                                        <DropShadowEffect BlurRadius="8" ShadowDepth="0" Opacity="0.3" Color="#7C3AED"/>
                                    </Border.Effect>
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="btnBorder" Property="Background">
                                            <Setter.Value>
                                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                    <GradientStop Color="#8B5CF6" Offset="0"/>
                                                    <GradientStop Color="#7C3AED" Offset="1"/>
                                                </LinearGradientBrush>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Statistics Cards -->
        <Grid Grid.Row="1" Margin="16,16,16,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Total P&L -->
            <Border Grid.Column="0" Style="{StaticResource StatCard}">
                <StackPanel>
                    <TextBlock Text="Total P&amp;L" Foreground="#808080" FontSize="12"/>
                    <TextBlock x:Name="TotalPnLDisplay" Text="+$0.00" FontSize="28" FontWeight="Bold"
                               Foreground="#10B981" Margin="0,8,0,0"/>
                    <TextBlock x:Name="TotalPnLPercent" Text="+0.00%" Foreground="#10B981" FontSize="13"/>
                </StackPanel>
            </Border>

            <!-- Total Trades -->
            <Border Grid.Column="1" Style="{StaticResource StatCard}">
                <StackPanel>
                    <TextBlock Text="Total Trades" Foreground="#808080" FontSize="12"/>
                    <TextBlock x:Name="TotalTradesDisplay" Text="0" FontSize="28" FontWeight="Bold"
                               Foreground="#FFFFFF" Margin="0,8,0,0"/>
                    <TextBlock x:Name="AvgTradesPerDay" Text="0/day avg" Foreground="#808080" FontSize="13"/>
                </StackPanel>
            </Border>

            <!-- Win Rate -->
            <Border Grid.Column="2" Style="{StaticResource StatCard}">
                <StackPanel>
                    <TextBlock Text="Win Rate" Foreground="#808080" FontSize="12"/>
                    <TextBlock x:Name="WinRateDisplay" Text="0%" FontSize="28" FontWeight="Bold"
                               Foreground="#00FFFF" Margin="0,8,0,0"/>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock x:Name="WinsDisplay" Text="0W" Foreground="#10B981" FontSize="13"/>
                        <TextBlock Text=" / " Foreground="#808080" FontSize="13"/>
                        <TextBlock x:Name="LossesDisplay" Text="0L" Foreground="#EF4444" FontSize="13"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Average Spread -->
            <Border Grid.Column="3" Style="{StaticResource StatCard}">
                <StackPanel>
                    <TextBlock Text="Avg Spread" Foreground="#808080" FontSize="12"/>
                    <TextBlock x:Name="AvgSpreadDisplay" Text="0.00%" FontSize="28" FontWeight="Bold"
                               Foreground="#F59E0B" Margin="0,8,0,0"/>
                    <TextBlock x:Name="BestSpreadDisplay" Text="Best: 0.00%" Foreground="#808080" FontSize="13"/>
                </StackPanel>
            </Border>

            <!-- Average Execution -->
            <Border Grid.Column="4" Style="{StaticResource StatCard}">
                <StackPanel>
                    <TextBlock Text="Avg Execution" Foreground="#808080" FontSize="12"/>
                    <TextBlock x:Name="AvgExecutionDisplay" Text="0ms" FontSize="28" FontWeight="Bold"
                               Foreground="#8B5CF6" Margin="0,8,0,0"/>
                    <TextBlock x:Name="FastestExecDisplay" Text="Fastest: 0ms" Foreground="#808080" FontSize="13"/>
                </StackPanel>
            </Border>

            <!-- Volume Traded -->
            <Border Grid.Column="5" Style="{StaticResource StatCard}">
                <StackPanel>
                    <TextBlock Text="Volume Traded" Foreground="#808080" FontSize="12"/>
                    <TextBlock x:Name="VolumeDisplay" Text="$0" FontSize="28" FontWeight="Bold"
                               Foreground="#FFFFFF" Margin="0,8,0,0"/>
                    <TextBlock x:Name="FeesDisplay" Text="Fees: $0" Foreground="#808080" FontSize="13"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Main Content -->
        <Grid Grid.Row="2" Margin="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="350"/>
            </Grid.ColumnDefinitions>

            <!-- Trade History List -->
            <Border Grid.Column="0" Background="#12121F" CornerRadius="12" Margin="0,0,8,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- List Header -->
                    <Border Grid.Row="0" Background="#1A1A2E" CornerRadius="12,12,0,0" Padding="16">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="140"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="Time" Foreground="#808080" FontWeight="SemiBold"/>
                            <TextBlock Grid.Column="1" Text="Pair" Foreground="#808080" FontWeight="SemiBold"/>
                            <TextBlock Grid.Column="2" Text="Buy Exchange" Foreground="#808080" FontWeight="SemiBold"/>
                            <TextBlock Grid.Column="3" Text="Sell Exchange" Foreground="#808080" FontWeight="SemiBold"/>
                            <TextBlock Grid.Column="4" Text="Spread" Foreground="#808080" FontWeight="SemiBold"/>
                            <TextBlock Grid.Column="5" Text="P&amp;L" Foreground="#808080" FontWeight="SemiBold"/>
                            <TextBlock Grid.Column="6" Text="Status" Foreground="#808080" FontWeight="SemiBold"/>
                            <TextBlock Grid.Column="7" Text="Exec Time" Foreground="#808080" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                        </Grid>
                    </Border>

                    <!-- Trade List -->
                    <ListView x:Name="TradeHistoryList" Grid.Row="1"
                              Style="{StaticResource PremiumListView}">
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="Padding" Value="16,12"/>
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListViewItem">
                                            <Border Background="{TemplateBinding Background}"
                                                    BorderBrush="#1A1A2E" BorderThickness="0,0,0,1"
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#1A1A2E"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </ListView.ItemContainerStyle>
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="140"/>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="120"/>
                                        <ColumnDefinition Width="120"/>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="80"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0" Text="{Binding TimeDisplay}"
                                               Foreground="#FFFFFF" FontSize="13"/>
                                    <TextBlock Grid.Column="1" Text="{Binding Symbol}"
                                               Foreground="#00FFFF" FontWeight="SemiBold" FontSize="13"/>
                                    <TextBlock Grid.Column="2" Text="{Binding BuyExchange}"
                                               Foreground="#FFFFFF" FontSize="13"/>
                                    <TextBlock Grid.Column="3" Text="{Binding SellExchange}"
                                               Foreground="#FFFFFF" FontSize="13"/>
                                    <TextBlock Grid.Column="4" Text="{Binding SpreadDisplay}"
                                               Foreground="#F59E0B" FontSize="13"/>
                                    <TextBlock Grid.Column="5" Text="{Binding PnLDisplay}"
                                               Foreground="{Binding PnLColor}" FontWeight="SemiBold" FontSize="13"/>
                                    <Border Grid.Column="6" Background="{Binding StatusBackground}"
                                            CornerRadius="4" Padding="8,2" HorizontalAlignment="Left">
                                        <TextBlock Text="{Binding StatusText}" Foreground="{Binding StatusColor}" FontSize="11"/>
                                    </Border>
                                    <TextBlock Grid.Column="7" Text="{Binding ExecutionTimeDisplay}"
                                               Foreground="#808080" FontSize="13" HorizontalAlignment="Right"/>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>

                    <!-- Empty State -->
                    <StackPanel x:Name="EmptyState" Grid.Row="1"
                                HorizontalAlignment="Center" VerticalAlignment="Center"
                                Visibility="Visible">
                        <TextBlock Text="No trades yet" FontSize="18" Foreground="#808080"
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="Start the scanner to find arbitrage opportunities"
                                   FontSize="13" Foreground="#606080" Margin="0,8,0,0"
                                   HorizontalAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Right Panel - Charts & Analytics -->
            <Grid Grid.Column="1" Margin="8,0,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- P&L Chart with Trade Markers -->
                <Border Grid.Row="0" Background="#12121F" CornerRadius="12" Margin="0,0,0,8" Padding="16">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
                            <TextBlock Text="P&amp;L Over Time" Style="{StaticResource SectionHeader}" Margin="0"/>
                            <TextBlock Text=" - Click on points to view details" Foreground="#606080" FontSize="12"
                                       VerticalAlignment="Center" Margin="8,0,0,0"/>
                        </StackPanel>

                        <lvc:CartesianChart Grid.Row="1"
                                           x:Name="PnLChart"
                                           Series="{Binding PnLSeries}"
                                           XAxes="{Binding XAxes}"
                                           YAxes="{Binding YAxes}"
                                           TooltipPosition="Top"
                                           ZoomMode="X"
                                           ChartPointPointerDown="PnLChart_ChartPointPointerDown"/>

                        <!-- Trade Detail Popup -->
                        <Border x:Name="TradeDetailPopup" Grid.Row="1"
                                Background="#1A1A2E" CornerRadius="12"
                                BorderBrush="#8B5CF6" BorderThickness="2"
                                Padding="16"
                                HorizontalAlignment="Center" VerticalAlignment="Center"
                                Visibility="Collapsed">
                            <Grid MinWidth="280">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- Header -->
                                <Grid Grid.Row="0">
                                    <TextBlock x:Name="PopupTitle" Text="Trade Details"
                                               FontSize="16" FontWeight="Bold" Foreground="#FFFFFF"/>
                                    <Button x:Name="ClosePopupBtn" Content="✕"
                                            HorizontalAlignment="Right"
                                            Background="Transparent" Foreground="#808080"
                                            BorderThickness="0" FontSize="14"
                                            Click="ClosePopup_Click" Cursor="Hand"/>
                                </Grid>

                                <!-- Trade Info -->
                                <StackPanel Grid.Row="1" Margin="0,16,0,0">
                                    <Grid Margin="0,4">
                                        <TextBlock Text="Time:" Foreground="#808080"/>
                                        <TextBlock x:Name="PopupTime" Text="-" Foreground="#FFFFFF" HorizontalAlignment="Right"/>
                                    </Grid>
                                    <Grid Margin="0,4">
                                        <TextBlock Text="Pair:" Foreground="#808080"/>
                                        <TextBlock x:Name="PopupPair" Text="-" Foreground="#00FFFF" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                                    </Grid>
                                    <Grid Margin="0,4">
                                        <TextBlock Text="Buy Exchange:" Foreground="#808080"/>
                                        <TextBlock x:Name="PopupBuyExchange" Text="-" Foreground="#FFFFFF" HorizontalAlignment="Right"/>
                                    </Grid>
                                    <Grid Margin="0,4">
                                        <TextBlock Text="Sell Exchange:" Foreground="#808080"/>
                                        <TextBlock x:Name="PopupSellExchange" Text="-" Foreground="#FFFFFF" HorizontalAlignment="Right"/>
                                    </Grid>
                                    <Grid Margin="0,4">
                                        <TextBlock Text="Spread:" Foreground="#808080"/>
                                        <TextBlock x:Name="PopupSpread" Text="-" Foreground="#F59E0B" HorizontalAlignment="Right"/>
                                    </Grid>
                                    <Grid Margin="0,4">
                                        <TextBlock Text="P&amp;L:" Foreground="#808080"/>
                                        <TextBlock x:Name="PopupPnL" Text="-" FontWeight="Bold" FontSize="16" HorizontalAlignment="Right"/>
                                    </Grid>
                                    <Grid Margin="0,4">
                                        <TextBlock Text="Execution:" Foreground="#808080"/>
                                        <TextBlock x:Name="PopupExecTime" Text="-" Foreground="#8B5CF6" HorizontalAlignment="Right"/>
                                    </Grid>
                                </StackPanel>

                                <!-- Status Badge -->
                                <Border x:Name="PopupStatusBadge" Grid.Row="2"
                                        Margin="0,16,0,0" CornerRadius="8"
                                        Padding="12,8" HorizontalAlignment="Center">
                                    <TextBlock x:Name="PopupStatus" Text="SUCCESS"
                                               FontWeight="Bold" FontSize="14"/>
                                </Border>
                            </Grid>
                        </Border>
                    </Grid>
                </Border>

                <!-- Performance by Pair -->
                <Border Grid.Row="1" Background="#12121F" CornerRadius="12" Margin="0,8,0,0" Padding="16">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Text="Performance by Pair" Style="{StaticResource SectionHeader}"/>

                        <ListView x:Name="PairPerformanceList" Grid.Row="1"
                                  Style="{StaticResource PremiumListView}">
                            <ListView.ItemContainerStyle>
                                <Style TargetType="ListViewItem">
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                    <Setter Property="Padding" Value="8"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ListViewItem">
                                                <Border Background="Transparent" Padding="{TemplateBinding Padding}">
                                                    <ContentPresenter/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ListView.ItemContainerStyle>
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="80"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Grid.Column="0" Text="{Binding Symbol}"
                                                   Foreground="#00FFFF" FontWeight="SemiBold"/>

                                        <!-- Progress Bar -->
                                        <Grid Grid.Column="1" Margin="12,0">
                                            <Border Background="#1A1A2E" CornerRadius="4" Height="8"/>
                                            <Border Background="{Binding BarColor}" CornerRadius="4" Height="8"
                                                    HorizontalAlignment="Left" Width="{Binding BarWidth}"/>
                                        </Grid>

                                        <StackPanel Grid.Column="2" Orientation="Horizontal">
                                            <TextBlock Text="{Binding PnLDisplay}" Foreground="{Binding PnLColor}"
                                                       FontWeight="SemiBold" Width="70" TextAlignment="Right"/>
                                            <TextBlock Text="{Binding TradeCount}" Foreground="#808080"
                                                       Margin="8,0,0,0" Width="40"/>
                                        </StackPanel>
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Grid>
                </Border>
            </Grid>
        </Grid>

    </Grid>
</UserControl>
